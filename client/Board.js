/**
 * Board Component
 * Renders the Connect 4 game board with a grid of slots.
 * 
 * Props:
 * - grid: Array representing the current state of the game board.
 * - onPlacePiece: Function to handle placing a piece in a column.
 * 
 * Usage:
 * <Board grid={currentGrid} onPlacePiece={handlePlacePiece} />
 */